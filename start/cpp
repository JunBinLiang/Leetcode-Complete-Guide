#include <iostream>
#include <vector>
using namespace std;
 
int solve(vector<int>&A){

}
 
int main()
{
    int t;cin>>t;
    while(t--){
        int n;cin>>n;
        vector<int>A;
        for(int i=0;i<n;i++){
            int a;cin>>a;
            A.push_back(a);
        }
        int res=solve(A);
        cout<<res<<endl;
    }
 
    return 0;
}
